#pragma once
#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <stdbool.h>

void InsertSort(int *a, int n);//数组的插入排序
void ShellSort(int *a, int n);
void SeletSort(int *a, int n);
void HeapSort(int *a, int n);
void BubbleSort(int *a, int n);

#pragma once
#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <stdbool.h>

void SingleInsertSort(int *a, int n);//插入排序
void InsertSort(int *a, int n);//数组的插入排序
void ShellSort(int *a, int n);